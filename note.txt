info 
1.  set(ref(db, "users////////abc"), something) is equivalent to set(ref(db, "users/abc"), something)

improvements:
1. update path should not be child of another update path
2. invalidate Record<number,unknown> when creating meta type