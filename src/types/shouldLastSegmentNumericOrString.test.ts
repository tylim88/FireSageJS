import { ShouldLastSegmentNumericOrString } from './shouldLastSegmentNumericOrString'
import { Users } from '../utilForTests'
import { IsSame } from './utils'

describe('test should last segment numeric or string', () => {
	it('positive test', () => {
		// type A = ShouldLastSegmentNumericOrString<Users, 'a/b', 'a/b'>
	})
})
